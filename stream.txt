USE WAREHOUSE AVIATION_WH;
USE DATABASE AVIATION_DB;
USE SCHEMA CORE;

--streams

CREATE OR REPLACE STREAM PASSENGER_STREAM
ON TABLE DIM_PASSENGER;

CREATE OR REPLACE STREAM FLIGHT_STREAM
ON TABLE DIM_FLIGHT;

CREATE OR REPLACE STREAM BOOKING_STREAM
ON TABLE FACT_BOOKING;

CREATE OR REPLACE STREAM BAGGAGE_STREAM
ON TABLE FACT_BAGGAGE;

CREATE OR REPLACE STREAM DELAY_ALERT_STREAM
ON TABLE FACT_DELAY_ALERT;

SHOW STREAMS;

SELECT * FROM BOOKING_STREAM;
SELECT * FROM PASSENGER_STREAM;
SELECT * FROM FLIGHT_STREAM;
