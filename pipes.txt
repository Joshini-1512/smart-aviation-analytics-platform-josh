USE WAREHOUSE AVIATION_WH;
USE DATABASE AVIATION_DB;
USE SCHEMA RAW;


CREATE OR REPLACE PIPE PASSENGER_PIPE
AUTO_INGEST = TRUE
AS
COPY INTO PASSENGER_RAW
FROM @RAW.S3_CSV_STAGE/passenger_data.csv
FILE_FORMAT = (FORMAT_NAME = CSV_FORMAT);


CREATE OR REPLACE PIPE FLIGHT_PIPE
AUTO_INGEST = TRUE
AS
COPY INTO FLIGHT_RAW
FROM @RAW.S3_CSV_STAGE/flight_data.csv
FILE_FORMAT = (FORMAT_NAME = CSV_FORMAT);


CREATE OR REPLACE PIPE BOOKING_PIPE
AUTO_INGEST = TRUE
AS
COPY INTO BOOKING_RAW
FROM @RAW.S3_CSV_STAGE/booking_data.csv
FILE_FORMAT = (FORMAT_NAME = CSV_FORMAT);


CREATE OR REPLACE PIPE BAGGAGE_PIPE
AUTO_INGEST = TRUE
AS
COPY INTO BAGGAGE_RAW
FROM @RAW.S3_CSV_STAGE/baggage_data.csv
FILE_FORMAT = (FORMAT_NAME = CSV_FORMAT);


CREATE OR REPLACE PIPE DELAY_ALERT_PIPE
AUTO_INGEST = TRUE
AS
COPY INTO DELAY_ALERT_JSON_RAW
FROM @RAW.S3_JSON_STAGE/delay_alert_data.json
FILE_FORMAT = (FORMAT_NAME = JSON_FORMAT);


SELECT COUNT(*) FROM PASSENGER_RAW;
SELECT COUNT(*) FROM FLIGHT_RAW;
SELECT COUNT(*) FROM BOOKING_RAW;
SELECT COUNT(*) FROM BAGGAGE_RAW;
SELECT COUNT(*) FROM DELAY_ALERT_RAW;

SHOW PIPES;